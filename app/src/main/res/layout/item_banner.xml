<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="com.demo.assignment.util.AppUtils" />

        <variable
            name="itemModel"
            type="com.demo.assignment.repository.model.MoviesModel" />
    </data>

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="192dp"
        android:layout_marginStart="8dp"
        android:layout_marginLeft="8dp"
        android:layout_marginEnd="8dp"
        android:layout_marginRight="8dp"
        android:layout_marginBottom="24dp"
        app:cardBackgroundColor="@color/placeholder_grey"
        app:cardCornerRadius="3dp"
        app:cardPreventCornerOverlap="false">

        <ImageView
            android:id="@+id/movie_poster"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:imageUrl="@{itemModel.backdropPath}"
            tools:background="@color/teal_200"
            tools:ignore="ContentDescription" />

        <View
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:alpha="0.7"
            android:background="@drawable/scrim" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:orientation="vertical"
            android:padding="16dp">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal">

                <TextView
                    android:id="@+id/movie_title"
                    style="@style/TextAppearance.AppCompat.Subhead.Inverse"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:ellipsize="end"
                    android:maxLength="40"
                    android:maxLines="1"
                    android:text="@{itemModel.title}"
                    tools:text="Doctor Who : The Day of the Doctor" />

                <TextView
                    android:id="@+id/movie_year"
                    style="@style/TextAppearance.AppCompat.Caption"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:alpha="0.4"
                    android:gravity="end"
                    android:maxLines="1"
                    android:text="@{AppUtils.formatYearLabel(itemModel)}"
                    android:textColor="@android:color/white"
                    android:textStyle="bold"
                    tools:text="2009 | EN" />

            </LinearLayout>

            <TextView
                android:id="@+id/movie_desc"
                style="@style/TextAppearance.AppCompat.Caption"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:alpha="0.7"
                android:ellipsize="end"
                android:maxLines="2"
                android:text="@{itemModel.overview}"
                android:textColor="@android:color/white"
                tools:text="Nowadays, user engagement is considered one of the most important metrics for the success of your app" />

        </LinearLayout>

    </androidx.cardview.widget.CardView>
</layout>